<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extension de la vue formulaire client -->
    <record id="view_partner_form_inherit_vehicle" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.vehicle</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <!-- Ajouter le bouton dans la button_box -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_vehicles" 
                        type="object" 
                        class="oe_stat_button" 
                        icon="fa-car"
                        invisible="vehicle_count == 0">
                    <field name="vehicle_count" widget="statinfo" string="Vehicles"/>
                </button>
            </xpath>
            
            <!-- Ajouter l'onglet véhicules -->
            <xpath expr="//notebook" position="inside">
                <page string="Vehicles" name="vehicles" invisible="is_company == False and parent_id != False">
                    <field name="vehicle_ids" context="{'default_partner_id': active_id}">
                        <tree editable="bottom">
                            <field name="license_plate"/>
                            <field name="brand_id" options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="model_id" options="{'no_create': True, 'no_create_edit': True}" domain="[('brand_id', '=', brand_id)]"/>
                            <field name="year"/>
                            <field name="current_mileage"/>
                            <field name="active"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

    <!-- Extension de la vue liste client pour afficher le nombre de véhicules -->
    <record id="view_partner_tree_inherit_vehicle" model="ir.ui.view">
        <field name="name">res.partner.tree.inherit.vehicle</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='phone']" position="after">
                <field name="vehicle_count" optional="hide" string="Vehicles"/>
            </xpath>
        </field>
    </record>

</odoo>